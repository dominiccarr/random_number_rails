#Primes and Random Numbers Example

This is a simple RoR demo application which can generate random numbers, generate a list of primes, and check if a number is prime. Intended for use by my students in NCI